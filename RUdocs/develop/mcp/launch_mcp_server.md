---
sidebar_position: 1
slug: /launch_mcp_server
---

# Запуск RAGFlow MCP сервера

Запустите MCP сервер из исходного кода или через Docker.

---

Сервер RAGFlow Model Context Protocol (MCP) разработан как независимый компонент для дополнения сервера RAGFlow. Обратите внимание, что MCP сервер должен работать совместно с корректно функционирующим сервером RAGFlow.

MCP сервер может запускаться в режиме self-host (по умолчанию) или в режиме host:

- **Режим self-host (самостоятельный хостинг)**:  
  При запуске MCP сервера в режиме self-host необходимо предоставить API ключ для аутентификации MCP сервера на сервере RAGFlow. В этом режиме MCP сервер имеет доступ *только* к наборам данных указанного арендатора (tenant) на сервере RAGFlow.
- **Режим host (хостинг)**:  
  В режиме host каждый MCP клиент может получить доступ к своим собственным наборам данных на сервере RAGFlow. Однако каждый запрос клиента должен содержать действительный API ключ для аутентификации клиента на сервере RAGFlow.

После установления соединения MCP сервер общается с клиентом в режиме MCP HTTP+SSE (Server-Sent Events), односторонне передавая ответы от сервера RAGFlow клиенту в реальном времени.

## Предварительные требования

1. Убедитесь, что RAGFlow обновлен до версии v0.18.0 или выше.
2. Имейте под рукой ваш API ключ RAGFlow. См. [Получение API ключа RAGFlow](../acquire_ragflow_api_key.md).

:::tip INFO
Если вы хотите опробовать наш MCP сервер без обновления RAGFlow, участник сообщества [yiminghub2024](https://github.com/yiminghub2024) 👏 делится своими рекомендуемыми шагами [здесь](#launch-an-mcp-server-without-upgrading-ragflow).
:::

## Запуск MCP сервера

Вы можете запустить MCP сервер либо из исходного кода, либо через Docker.

### Запуск из исходного кода

1. Убедитесь, что сервер RAGFlow версии v0.18.0+ корректно запущен.
2. Запустите MCP сервер:

```bash
# Запуск MCP сервера в режиме self-host, выполните одну из команд:
uv run mcp/server/server.py --host=127.0.0.1 --port=9382 --base-url=http://127.0.0.1:9380 --api-key=ragflow-xxxxx
# uv run mcp/server/server.py --host=127.0.0.1 --port=9382 --base-url=http://127.0.0.1:9380 --mode=self-host --api-key=ragflow-xxxxx

# Для запуска MCP сервера в режиме host выполните:
# uv run mcp/server/server.py --host=127.0.0.1 --port=9382 --base-url=http://127.0.0.1:9380 --mode=host
```

Где:

- `host`: адрес хоста MCP сервера.
- `port`: порт прослушивания MCP сервера.
- `base_url`: адрес запущенного сервера RAGFlow.
- `mode`: режим запуска.
  - `self-host`: (по умолчанию) режим self-host.
  - `host`: режим host.
- `api_key`: требуется в режиме self-host для аутентификации MCP сервера на сервере RAGFlow. Инструкции по получению API ключа см. [здесь](../acquire_ragflow_api_key.md).

### Транспорты

Сервер RAGFlow MCP поддерживает два транспорта: устаревший SSE транспорт (доступен по адресу `/sse`), введённый 5 ноября 2024 года и устаревший с 26 марта 2025 года, и streamable-HTTP транспорт (доступен по адресу `/mcp`). По умолчанию включены устаревший SSE транспорт и streamable HTTP транспорт с JSON ответами. Чтобы отключить любой из транспортов, используйте флаги `--no-transport-sse-enabled` или `--no-transport-streamable-http-enabled`. Для отключения JSON ответов в streamable HTTP транспорте используйте флаг `--no-json-response`.

### Запуск через Docker

#### 1. Включение MCP сервера

MCP сервер разработан как дополнительный компонент к серверу RAGFlow и по умолчанию отключён. Чтобы включить MCP сервер:

1. Перейдите в файл **docker/docker-compose.yml**.
2. Раскомментируйте секцию `services.ragflow.command`, как показано ниже:

```yaml {6-13}
  services:
    ragflow:
      ...
      image: ${RAGFLOW_IMAGE}
      # Пример конфигурации для настройки MCP сервера:
      command:
        - --enable-mcpserver
        - --mcp-host=0.0.0.0
        - --mcp-port=9382
        - --mcp-base-url=http://127.0.0.1:9380
        - --mcp-script-path=/ragflow/mcp/server/server.py
        - --mcp-mode=self-host
        - --mcp-host-api-key=ragflow-xxxxxxx
        # Опциональные флаги транспорта для RAGFlow MCP сервера.
        # Если вы устанавливаете `mcp-mode` в `host`, необходимо добавить флаг --no-transport-streamable-http-enabled, так как streamable-HTTP транспорт пока не поддерживается в режиме host.
        # По умолчанию включены устаревший SSE транспорт и streamable-HTTP транспорт с JSON ответами.
        # Для отключения конкретного транспорта или JSON ответов для streamable-HTTP транспорта используйте соответствующие флаги:
        #   - --no-transport-sse-enabled # Отключает устаревший SSE endpoint (/sse)
        #   - --no-transport-streamable-http-enabled # Отключает streamable-HTTP транспорт (доступен по /mcp)
        #   - --no-json-response # Отключает JSON ответы для streamable-HTTP транспорта
```

Где:

- `mcp-host`: адрес хоста MCP сервера.
- `mcp-port`: порт прослушивания MCP сервера.
- `mcp-base-url`: адрес запущенного сервера RAGFlow.
- `mcp-script-path`: путь к основному скрипту MCP сервера.
- `mcp-mode`: режим запуска.
  - `self-host`: (по умолчанию) режим self-host.
  - `host`: режим host.
- `mcp-host-api_key`: требуется в режиме self-host для аутентификации MCP сервера на сервере RAGFlow. Инструкции по получению API ключа см. [здесь](../acquire_ragflow_api_key.md).

:::tip INFO
Если вы устанавливаете `mcp-mode` в `host`, необходимо добавить флаг `--no-transport-streamable-http-enabled`, так как streamable-HTTP транспорт пока не поддерживается в режиме host.
:::

#### 2. Запуск сервера RAGFlow с MCP сервером

Выполните команду `docker compose -f docker-compose.yml up` для запуска сервера RAGFlow вместе с MCP сервером.

*Следующее ASCII-искусство подтверждает успешный запуск:*

```bash
  docker-ragflow-cpu-1  | Starting MCP Server on 0.0.0.0:9382 with base URL http://127.0.0.1:9380...
  docker-ragflow-cpu-1  | Starting 1 task executor(s) on host 'dd0b5e07e76f'...
  docker-ragflow-cpu-1  | 2025-04-18 15:41:18,816 INFO     27 ragflow_server log path: /ragflow/logs/ragflow_server.log, log levels: {'peewee': 'WARNING', 'pdfminer': 'WARNING', 'root': 'INFO'}
  docker-ragflow-cpu-1  | 
  docker-ragflow-cpu-1  | __  __  ____ ____       ____  _____ ______     _______ ____
  docker-ragflow-cpu-1  | |  \/  |/ ___|  _ \     / ___|| ____|  _ \ \   / / ____|  _ \
  docker-ragflow-cpu-1  | | |\/| | |   | |_) |    \___ \|  _| | |_) \ \ / /|  _| | |_) |
  docker-ragflow-cpu-1  | | |  | | |___|  __/      ___) | |___|  _ < \ V / | |___|  _ <
  docker-ragflow-cpu-1  | |_|  |_|\____|_|        |____/|_____|_| \_\ \_/  |_____|_| \_\
  docker-ragflow-cpu-1  |     
  docker-ragflow-cpu-1  | MCP launch mode: self-host
  docker-ragflow-cpu-1  | MCP host: 0.0.0.0
  docker-ragflow-cpu-1  | MCP port: 9382
  docker-ragflow-cpu-1  | MCP base_url: http://127.0.0.1:9380
  docker-ragflow-cpu-1  | INFO:     Started server process [26]
  docker-ragflow-cpu-1  | INFO:     Waiting for application startup.
  docker-ragflow-cpu-1  | INFO:     Application startup complete.
  docker-ragflow-cpu-1  | INFO:     Uvicorn running on http://0.0.0.0:9382 (Press CTRL+C to quit)
  docker-ragflow-cpu-1  | 2025-04-18 15:41:20,469 INFO     27 found 0 gpus
  docker-ragflow-cpu-1  | 2025-04-18 15:41:23,263 INFO     27 init database on cluster mode successfully
  docker-ragflow-cpu-1  | 2025-04-18 15:41:25,318 INFO     27 load_model /ragflow/rag/res/deepdoc/det.onnx uses CPU
  docker-ragflow-cpu-1  | 2025-04-18 15:41:25,367 INFO     27 load_model /ragflow/rag/res/deepdoc/rec.onnx uses CPU
  docker-ragflow-cpu-1  |         ____   ___    ______ ______ __               
  docker-ragflow-cpu-1  |        / __ \ /   |  / ____// ____// /____  _      __
  docker-ragflow-cpu-1  |       / /_/ // /| | / / __ / /_   / // __ \| | /| / /
  docker-ragflow-cpu-1  |      / _, _// ___ |/ /_/ // __/  / // /_/ /| |/ |/ / 
  docker-ragflow-cpu-1  |     /_/ |_|/_/  |_|\____//_/    /_/ \____/ |__/|__/                             
  docker-ragflow-cpu-1  | 
  docker-ragflow-cpu-1  |     
  docker-ragflow-cpu-1  | 2025-04-18 15:41:29,088 INFO     27 RAGFlow version: v0.18.0-285-gb2c299fa full
  docker-ragflow-cpu-1  | 2025-04-18 15:41:29,088 INFO     27 project base: /ragflow
  docker-ragflow-cpu-1  | 2025-04-18 15:41:29,088 INFO     27 Current configs, from /ragflow/conf/service_conf.yaml:
  docker-ragflow-cpu-1  |  ragflow: {'host': '0.0.0.0', 'http_port': 9380}
  ...
  docker-ragflow-cpu-1  |  * Running on all addresses (0.0.0.0)
  docker-ragflow-cpu-1  |  * Running on http://127.0.0.1:9380
  docker-ragflow-cpu-1  |  * Running on http://172.19.0.6:9380
  docker-ragflow-cpu-1  |   ______           __      ______                     __            
  docker-ragflow-cpu-1  |  /_  __/___ ______/ /__   / ____/  _____  _______  __/ /_____  _____
  docker-ragflow-cpu-1  |   / / / __ `/ ___/ //_/  / __/ | |/_/ _ \/ ___/ / / / __/ __ \/ ___/
  docker-ragflow-cpu-1  |  / / / /_/ (__  ) ,<    / /____>  </  __/ /__/ /_/ / /_/ /_/ / /    
  docker-ragflow-cpu-1  | /_/  \__,_/____/_/|_|  /_____/_/|_|\___/\___/\__,_/\__/\____/_/                               
  docker-ragflow-cpu-1  |     
  docker-ragflow-cpu-1  | 2025-04-18 15:41:34,501 INFO     32 TaskExecutor: RAGFlow version: v0.18.0-285-gb2c299fa full
  docker-ragflow-cpu-1  | 2025-04-18 15:41:34,501 INFO     32 Use Elasticsearch http://es01:9200 as the doc engine.
  ...
```

#### Запуск MCP сервера без обновления RAGFlow

:::info KUDOS
Этот раздел подготовлен нашим участником сообщества [yiminghub2024](https://github.com/yiminghub2024). 👏
:::

1. Подготовьте все файлы и каталоги, специфичные для MCP.  
   i. Скопируйте каталог [mcp/](https://github.com/infiniflow/ragflow/tree/main/mcp) в вашу локальную рабочую директорию.  
   ii. Скопируйте [docker/docker-compose.yml](https://github.com/infiniflow/ragflow/blob/main/docker/docker-compose.yml) локально.  
   iii. Скопируйте [docker/entrypoint.sh](https://github.com/infiniflow/ragflow/blob/main/docker/entrypoint.sh) локально.  
   iv. Установите необходимые зависимости с помощью `uv`:  
       - Выполните `uv add mcp` или  
       - Скопируйте [pyproject.toml](https://github.com/infiniflow/ragflow/blob/main/pyproject.toml) локально и выполните `uv sync --python 3.10`.
2. Отредактируйте **docker-compose.yml** для включения MCP (по умолчанию отключён).
3. Запустите MCP сервер:

```bash
docker compose -f docker-compose.yml up -d
```

### Проверка статуса MCP сервера

Выполните следующую команду для просмотра логов сервера RAGFlow и MCP сервера:

```bash
docker logs docker-ragflow-cpu-1
```

## Вопросы безопасности

Так как технология MCP находится на ранней стадии развития и официальных рекомендаций по аутентификации и авторизации пока нет, RAGFlow в настоящее время использует [API ключ](./acquire_ragflow_api_key.md) для проверки подлинности операций, описанных выше. Однако в публичных средах такое временное решение может подвергнуть ваш MCP сервер потенциальным сетевым атакам. Поэтому при запуске локального SSE сервера рекомендуется привязывать его только к localhost (`127.0.0.1`), а не ко всем интерфейсам (`0.0.0.0`).

Для дополнительной информации смотрите [официальную документацию MCP](https://modelcontextprotocol.io/docs/concepts/transports#security-considerations).

## Часто задаваемые вопросы

### Когда использовать API ключ для аутентификации?

Использование API ключа зависит от режима работы вашего MCP сервера.

- **Режим self-host (по умолчанию)**:  
  При запуске MCP сервера в режиме self-host необходимо предоставить API ключ для аутентификации с сервером RAGFlow:  
  - При запуске из исходного кода включите API ключ в команду запуска.  
  - При запуске через Docker обновите API ключ в файле **docker/docker-compose.yml**.
- **Режим host**:  
  Если ваш MCP сервер работает в режиме host, включайте API ключ в заголовки (`headers`) ваших клиентских запросов для аутентификации клиента на сервере RAGFlow. Пример доступен [здесь](https://github.com/infiniflow/ragflow/blob/main/mcp/client/client.py).