---
sidebar_position: 1
slug: /build_docker_image
---

# Сборка Docker-образа RAGFlow
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

Руководство, объясняющее, как собрать Docker-образ RAGFlow из исходного кода. Следуя этому руководству, вы сможете создать локальный Docker-образ, который можно использовать для разработки, отладки или тестирования.

## Целевая аудитория

- Разработчики, которые добавили новые функции или изменили существующий код и нуждаются в Docker-образе для просмотра и отладки своих изменений.
- Разработчики, желающие собрать Docker-образ RAGFlow для платформы ARM64.
- Тестировщики, стремящиеся изучить последние возможности RAGFlow в Docker-образе.

## Требования

- CPU &ge; 4 ядра
- ОЗУ &ge; 16 ГБ
- Диск &ge; 50 ГБ
- Docker &ge; 24.0.0 & Docker Compose &ge; v2.26.1

## Сборка Docker-образа

<Tabs
  defaultValue="without"
  values={[
    {label: 'Собрать Docker-образ без embedding моделей', value: 'without'},
    {label: 'Собрать Docker-образ с embedding моделями', value: 'including'}
  ]}>
  <TabItem value="without">

Этот образ занимает примерно 2 ГБ и использует внешние LLM и embedding сервисы.

:::danger ВАЖНО
- Хотя мы также тестируем RAGFlow на платформах ARM64, мы не поддерживаем Docker-образы RAGFlow для ARM. Тем не менее, вы можете самостоятельно собрать образ на хост-машине с `linux/arm64` или `darwin/arm64`.
- Для платформ ARM64 обновите версию `xgboost` в файле **pyproject.toml** до `1.6.0` и убедитесь, что **unixODBC** установлен корректно.
:::

```bash
git clone https://github.com/infiniflow/ragflow.git
cd ragflow/
uv run download_deps.py
docker build -f Dockerfile.deps -t infiniflow/ragflow_deps .
docker build -f Dockerfile -t infiniflow/ragflow:nightly .
```


  </TabItem>
</Tabs>

## Запуск сервиса RAGFlow из Docker для MacOS

После сборки образа infiniflow/ragflow:nightly вы готовы запустить полнофункциональный сервис RAGFlow со всеми необходимыми компонентами, такими как Elasticsearch, MySQL, MinIO, Redis и другими.

## Пример: Apple M2 Pro (Sequoia)

1. Редактирование конфигурации Docker Compose

Откройте файл `docker/.env`. Найдите параметр `RAGFLOW_IMAGE` и измените ссылку на образ с `infiniflow/ragflow:v0.21.1` на `infiniflow/ragflow:nightly`, чтобы использовать предварительно собранный образ.


2. Запуск сервиса

```bash
cd docker
$ docker compose -f docker-compose-macos.yml up -d
```

3. Доступ к сервису RAGFlow

После завершения настройки откройте веб-браузер и перейдите по адресу http://127.0.0.1 или \<IP_ADDRESS\> вашего сервера; (порт по умолчанию \<PORT\> = 80). Вы попадёте на приветственную страницу RAGFlow. Приятного пользования!🍻