---
sidebar_position: 9
slug: /tracing
---

# Трассировка (Tracing)

Observability (Наблюдаемость) и Tracing (Трассировка) с Langfuse.

---

:::info KUDOS
Этот документ подготовлен нашим участником сообщества [jannikmaierhoefer](https://github.com/jannikmaierhoefer). 👏
:::

RAGFlow поставляется с встроенной интеграцией [Langfuse](https://langfuse.com), которая позволяет **просматривать и отлаживать каждый шаг извлечения и генерации** ваших RAG pipeline в почти реальном времени.

Langfuse сохраняет трассы (traces), спаны (spans) и полезные нагрузки prompt в специально созданном backend для наблюдаемости и предоставляет возможности фильтрации и визуализации поверх них.

:::info NOTE
• RAGFlow **≥ 0.21.1** (содержит коннектор Langfuse)  
• Рабочее пространство Langfuse (облачное или self-hosted) с _Project Public Key_ и _Secret Key_
:::

---

## 1. Получите ваши учетные данные Langfuse

1. Войдите в вашу панель управления Langfuse.  
2. Откройте **Settings ▸ Projects** и создайте новый проект или выберите существующий.  
3. Скопируйте **Public Key** и **Secret Key**.  
4. Запомните Langfuse **host** (например, `https://cloud.langfuse.com`). Используйте базовый URL вашей собственной установки, если вы используете self-host.

> Ключи _привязаны к проекту_: одна пара ключей достаточно для всех окружений, которые должны писать в один и тот же проект.

---

## 2. Добавьте ключи в RAGFlow

RAGFlow хранит учетные данные _для каждого арендатора (tenant)_. Вы можете настроить их через веб-интерфейс или HTTP API.

1. Войдите в RAGFlow и нажмите на ваш аватар в правом верхнем углу.  
2. Выберите **API ▸ Пролистайте вниз ▸ Langfuse Configuration**.  
3. Введите ваш Langfuse **Host**, **Public Key** и **Secret Key**.  
4. Нажмите **Save**.

![Пример трассы RAGFlow в Langfuse](https://langfuse.com/images/docs/ragflow/ragflow-configuration.gif)

После сохранения RAGFlow начнет автоматически отправлять трассы — изменение кода не требуется.

---

## 3. Запустите pipeline и наблюдайте трассы

1. Выполните любой чат или retrieval pipeline в RAGFlow (например, демонстрацию Quickstart).  
2. Откройте ваш проект в Langfuse ▸ **Traces**.  
3. Отфильтруйте по **name ~ `ragflow-*`** (RAGFlow добавляет префикс `ragflow-` к каждой трассе).

Для каждого пользовательского запроса вы увидите:

• **trace** — представляющий общий запрос  
• **spans** для шагов извлечения, ранжирования и генерации  
• полные **prompts**, **retrieved documents** и **LLM responses** в виде метаданных

![Пример трассы RAGFlow в Langfuse](https://langfuse.com/images/docs/ragflow/ragflow-trace-frame.png)

([Пример трассы в Langfuse](https://cloud.langfuse.com/project/cloramnkj0002jz088vzn1ja4/traces/0bde9629-4251-4386-b583-26101b8e7561?timestamp=2025-05-09T19%3A15%3A37.797Z&display=details&observation=823997d8-ac40-40f3-8e7b-8aa6753b499e))

:::tip NOTE
Используйте режим сравнения (diff view) Langfuse для сравнения версий prompt или углубляйтесь в длительные retrieval, чтобы выявить узкие места.
:::